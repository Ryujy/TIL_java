StringTokenizer 생성자
1. StringTokenizer(String s)
2. StringTokenizer(String s, String delim): 설정한 구획문자를 기준으로 끊어준다.
3. StringTokenizer(String s, String delim, boolean returnDelims): returnDelims가 true 이면 구획문자도 토큰으로 간주한다.

StringTokenizer의 주요 메소드

1. .countTokens(): 토큰의 개수 리턴
2. .nextToken(): 다음 토큰을 리턴, 이전 토큰은 제거.
3. .nextToken(String delim): 구획문자(deliimilter)를 기준으로 token리턴
4. .hasMoreToken(): 리턴할 다음 토큰이 있으면 true, 없으면 false를 리턴

